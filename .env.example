# respond.io Gateway POC
RESPOND_IO_API_KEY=sdfklsjdf
RESPOND_IO_CHANNEL_ID=your_channel_id

# Server
PORT=3001
NODE_ENV=development

# Polling 설정 (Growth 플랜용)
# true로 설정하면 webhook 없이 polling으로 메시지 수신
POLLING_ENABLED=false
# 모니터링할 contact IDs (쉼표로 구분, 예: 123,456,789)
POLLING_CONTACTS=
